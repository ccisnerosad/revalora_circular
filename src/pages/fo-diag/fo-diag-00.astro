---
import Layout from '@layouts/LayoutFo.astro';
import formatMetadata from '@data/diag/format_diag.json';

interface MetadataItem {
  ref: string;
  line: string;
  aspect: string;
  variable: string;
  method: string;
  frequency: string;
  responsible: string;
  format_id: string;
  format_name: string;
}

const groupedData = (formatMetadata as MetadataItem[]).reduce((acc, item) => {
  if (!acc[item.line]) {
    acc[item.line] = [];
  }
  acc[item.line].push(item);
  return acc;
}, {} as Record<string, MetadataItem[]>);

const sections = Object.keys(groupedData);
---

<Layout title="FO-DIAG-00: Matriz de Levantamiento">
  <div class="min-h-screen bg-gray-50 dark:bg-gray-900 pb-12">
    <!-- Header with Breadcrumb -->
    <div class="bg-white dark:bg-gray-800 shadow-sm border-b border-gray-200 dark:border-gray-700">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <div class="flex items-center space-x-2 text-sm text-gray-500 dark:text-gray-400 mb-2">
          <a href="/diagnostico_residuos" class="hover:text-brand-600 dark:hover:text-brand-400 transition-colors">Formatos</a>
          <span>/</span>
          <span class="text-gray-900 dark:text-white font-medium">FO-DIAG-00</span>
        </div>
        <div class="flex justify-between items-center">
          <div>
            <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Matriz de Levantamiento en Campo</h1>
            <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">Plan Maestro de Diagnóstico y Control</p>
          </div>
          <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-brand-100 text-brand-800 dark:bg-brand-900 dark:text-brand-300">
            <span class="w-2 h-2 bg-brand-500 rounded-full mr-2"></span>
            Activo
          </span>
        </div>
      </div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      
      <!-- Methodological Note -->
      <div class="bg-blue-50 dark:bg-blue-900/20 border-l-4 border-blue-500 p-4 mb-8 rounded-r-lg">
        <div class="flex">
          <div class="shrink-0">
            <svg class="h-5 w-5 text-blue-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-blue-700 dark:text-blue-300">
              <span class="font-bold">Nota Metodológica:</span> Esta matriz responde a un enfoque integrado. La información se recopila de manera simultánea durante los recorridos. Cada recorrido activa varios registros de forma paralela garantizando coherencia entre caracterización, operación, infraestructura y actores.
            </p>
          </div>
        </div>
      </div>

      <div class="space-y-8">
        {sections.map((sectionTitle) => (
          <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 overflow-hidden">
            <div class="bg-gray-50 dark:bg-gray-700/50 px-6 py-4 border-b border-gray-200 dark:border-gray-700 flex justify-between items-center">
              <h2 class="text-lg font-bold text-gray-900 dark:text-white flex items-center">
                <span class="bg-brand-100 text-brand-800 dark:bg-brand-900 dark:text-brand-300 text-xs font-bold px-2 py-1 rounded mr-3">
                  {groupedData[sectionTitle][0].ref}
                </span>
                {sectionTitle}
              </h2>
            </div>
            
            <!-- Desktop Table View -->
            <div class="hidden md:block overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
                <thead class="bg-gray-50 dark:bg-gray-800">
                  <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-1/4">Aspecto a Evaluar</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-1/4">Variable / Indicador</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-1/6">Método</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-1/6">Frecuencia</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-1/6">Formato / Evidencia</th>
                  </tr>
                </thead>
                <tbody class="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
                  {groupedData[sectionTitle].map((item) => (
                    <tr class="hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                      <td class="px-6 py-4 text-sm font-medium text-gray-900 dark:text-white">
                        {item.aspect}
                      </td>
                      <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">
                        {item.variable}
                      </td>
                      <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">
                        {item.method}
                      </td>
                      <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">
                        {item.frequency}
                      </td>
                      <td class="px-6 py-4 text-sm">
                        <a href={`/fo/${item.format_id.toLowerCase()}`} class="inline-flex items-center px-2.5 py-1.5 border border-transparent text-xs font-medium rounded text-brand-700 bg-brand-100 hover:bg-brand-200 dark:text-brand-300 dark:bg-brand-900/50 dark:hover:bg-brand-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-500 transition-colors">
                          {item.format_id}
                          <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 ml-1" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                          </svg>
                        </a>
                        <div class="text-xs text-gray-400 mt-1 truncate max-w-[150px]" title={item.format_name}>
                          {item.format_name}
                        </div>
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>

            <!-- Mobile Card View -->
            <div class="md:hidden divide-y divide-gray-200 dark:divide-gray-700">
              {groupedData[sectionTitle].map((item) => (
                <div class="p-4 space-y-3">
                  <div class="flex justify-between items-start">
                    <h3 class="text-sm font-medium text-gray-900 dark:text-white">{item.aspect}</h3>
                    <a href={`/fo/${item.format_id.toLowerCase()}`} class="inline-flex items-center px-2 py-1 border border-transparent text-xs font-medium rounded text-brand-700 bg-brand-100 dark:text-brand-300 dark:bg-brand-900/50">
                      {item.format_id}
                    </a>
                  </div>
                  <div class="grid grid-cols-2 gap-2 text-xs">
                    <div>
                      <span class="block text-gray-400">Variable</span>
                      <span class="text-gray-600 dark:text-gray-300">{item.variable}</span>
                    </div>
                    <div>
                      <span class="block text-gray-400">Método</span>
                      <span class="text-gray-600 dark:text-gray-300">{item.method}</span>
                    </div>
                    <div class="col-span-2">
                      <span class="block text-gray-400">Responsable</span>
                      <span class="text-gray-600 dark:text-gray-300">{item.responsible}</span>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</Layout>